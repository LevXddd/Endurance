#include <cstdlib>
#include <iostream>
#define GREETING "WELCOME TO THE PROGRAM"
#define RESULT_LABEL "The double of x and y will be: "
void functionONE() { std::cout << GREETING; }
int functionTWO(int x, int y) {
  std::cout << RESULT_LABEL << x * y << '\n';
  return EXIT_SUCCESS;
}
int main() {
  functionONE();
  std::cout << "Enter the integrals: ";
  int x{};
  int y{};
  std::cin >> x >> y;
  functionTWO(x, y);
  return EXIT_SUCCESS;
}

